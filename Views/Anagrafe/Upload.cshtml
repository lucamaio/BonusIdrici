@{
    ViewData["Title"] = "Carica Anagrafe";
}

<div class="container my-5">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card shadow rounded-4 border-0">
                <div class="card-header header-classic text-white text-center rounded-top-4">
                    <h3 class="mb-0">Carica Anagrafe</h3>
                </div>
                <div class="card-body p-4">
                    <form asp-controller="Anagrafe" asp-action="Upload" method="post" enctype="multipart/form-data" onsubmit="showLoader()">
                           @* --- SELECT ENTE QUI --- *@
                        <div class="mb-3">
                            <label for="selectedEnteId" class="form-label">Seleziona l'Ente</label>
                            <select id="selectedEnteId" name="selectedEnteId" class="form-select" required>
                                <option value="">-- Seleziona un Ente --</option>
                                @if (ViewBag.Enti != null)
                                {
                                    foreach (var ente in ViewBag.Enti)
                                    {
                                        <option value="@ente.id">@ente.nome</option>
                                    }
                                }
                            </select>
                        </div>

                        <div class="alert alert-secondary small" role="alert">
                            Il mese e l'anno indicano la fotografia anagrafica da usare nelle elaborazioni Bonus Idrico dello stesso periodo. Se la snapshot esiste gia, verra sostituita dal nuovo import.
                        </div>

                        <div class="row g-3">
                            <div class="col-md-6">
                                <label for="meseRiferimento" class="form-label">Mese riferimento anagrafe</label>
                                <select id="meseRiferimento" name="meseRiferimento" class="form-select" required>
                                    <option value="">-- Seleziona mese --</option>
                                    @for (var mese = 1; mese <= 12; mese++)
                                    {
                                        <option value="@mese">@System.Globalization.CultureInfo.CurrentCulture.DateTimeFormat.GetMonthName(mese)</option>
                                    }
                                </select>
                            </div>
                            <div class="col-md-6">
                                <label for="annoRiferimento" class="form-label">Anno riferimento anagrafe</label>
                                <input type="number" id="annoRiferimento" name="annoRiferimento" class="form-control" min="2021" max="@(DateTime.Now.Year + 1)" value="@DateTime.Now.Year" required>
                            </div>
                        </div>

                          <div class="mb-3">
                            <label for="csv_file" class="form-label">Seleziona file Anagrafe (formato CSV)</label>
                            <input type="file" name="csv_file" class="form-control" required accept=".csv">
                        </div>
                        <div class="d-grid">
                            <button type="submit" class="btn btn-classic">Carica File</button>
                        </div>
                    </form>

                      <!-- Loader nascosto inizialmente -->
                    <div id="loader" class="text-center mt-3" style="display:none;">
                        <div class="spinner-border text-primary" role="status">
                            <span class="visually-hidden">Caricamento...</span>
                        </div>
                        <p class="mt-2">Elaborazione in corso, attendere...</p>
                    </div>

                    @if (!string.IsNullOrEmpty(ViewBag.Message))
                    {
                        <div class="alert alert-info mt-3" role="alert">
                            @ViewBag.Message
                        </div>
                    }
                </div>
            </div>
        </div>
    </div>
</div>
@section Scripts {
    <script>
        function showLoader() {
            document.getElementById("loader").style.display = "block";
        }
    </script>
}
